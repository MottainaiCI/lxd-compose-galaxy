# Author: Daniele Rondina, geaaru@sabayonlinux.org
# Description: Setup Simplestreams builder container

version: "1"

template_engine:
  engine: "mottainai"

include_profiles_files:
- ../common/profiles/net-mottainai0.yml
- ../common/profiles/default.yml
- ../common/profiles/flavor-medium.yml
- ../common/profiles/loop.yml
- ../common/profiles/docker.yml
- ../common/profiles/privileged.yml

include_networks_files:
- ../common/networks/mottainai0.yml

include_commands_files:
- commands/build-funtoo-iso.yml
- commands/build-funtoo-iso-makeiso.yml
- commands/build-gnome-iso.yml

projects:

  - name: "mocaccino-dev-services"
    description: |
      List of groups to run Mocaccino OS Build tasks

    include_env_files:

    vars:
      - envs:
          LUET_NOLOCK: "true"
          LUET_YES: "true"
          internal_domain: "mottainai.local"

    hooks:
      - event: post-node-creation
        commands:
          # DHCP seems slow
          - sleep 2
          - apt-get install -y xorriso squashfs-tools dosfstools file curl

      - event: post-node-creation
        disable: true
        flags:
          - luet_master
        commands:
          # Using luet master
          - luet uninstall system/luet-geaaru
          - curl https://get.mocaccino.org/luet/get_luet_root.sh | sudo sh

      - event: post-node-creation
        commands:
          - luet install utils/yq utils/jq system/luet-extensions system/luet-devkit extension/makeiso
          - luet cleanup

      - event: pre-node-sync
        flags:
          - upgrade
        commands:
          - luet upgrade && luet cleanup

      - event: pre-node-sync
        flags:
          - install_geniso
        commands:
          - >-
            eval 'rm -rf /extensions || true' &&
            git clone https://github.com/Luet-lab/extensions.git /extensions &&
            cd /extensions/extensions/geniso && make install

    groups:
      - name: "mocaccino::iso-geniso"
        description: |
          Build Mocaccino ISO with luet-geniso extension.


        common_profiles:
          - default
          - net-mottainai0
          - docker
          - privileged
          - loop

        # Create the environment container as ephemeral or not.
        ephemeral: true
        connection: "{{ .Values.connection }}"

        nodes:
          - name: mocaccino-iso
            image_source: "mocaccino/builder"
            image_remote_server: "keybase-geaaru"

            hooks:
              - event: pre-node-sync
                flags:
                  - clone_repo
                commands:
                  - eval 'rm -rf /repo || true'
                  - echo "Cloning ${repo} for branch ${repo_branch}..."
                  - git clone ${repo} -b ${repo_branch} /repo
                  - cd /repo/ && eval 'rm -rf $TMPDIR || true' && mkdir $TMPDIR -p

              - event: post-node-sync
                flags:
                  - build_iso
                commands:
                  - >-
                    cd /repo/ &&
                    if [ ! -e $TMPDIR ] ; then mkdir $TMPDIR -p ; fi &&
                    bash -x luet-geniso-isospec ${spec_file} &&
                    ls -ltr &&
                    mkdir iso &&
                    mv *.iso *.iso.* iso/

              - event: post-node-sync
                node: host
                flags:
                  - pull_iso
                commands:
                  - >-
                    eval 'rm -rf iso || true' &&
                    lxc file pull {{ .Values.connection }}:mocaccino-iso/repo/iso/ . --recursive

      - name: "mocaccino::iso-makeiso"
        description: |
          Build Mocaccino ISO with luet-makeiso extension.

          Additional render variable to use:

            * local_luet_binary: to set with the path of the local luet binary to push
              in the container. If not set it's used official binary.

        common_profiles:
          - default
          - net-mottainai0
          - docker
          - privileged
          - loop

        # Create the environment container as ephemeral or not.
        ephemeral: true
        connection: "{{ .Values.connection }}"

        nodes:
          - name: mocaccino-makeiso
            image_source: "mocaccino/builder"
            image_remote_server: "keybase-geaaru"

            hooks:
              - event: pre-node-sync
                flags:
                  - clone_repo
                commands:
                  - eval 'rm -rf /repo || true'
                  - echo "Cloning ${repo} for branch ${repo_branch}..."
                  - git clone ${repo} -b ${repo_branch} /repo
                  - cd /repo/ && eval 'rm -rf $TMPDIR || true' && mkdir $TMPDIR -p

      {{ if .Values.local_luet_binary }}
              - event: post-node-sync
                node: host
                commands:
                  - lxc file push {{ .Values.local_luet_binary }} {{ .Values.connection }}:mocaccino-makeiso/tmp/luet

              - event: post-node-sync
                commands:
                  - mv /tmp/luet /usr/bin/luet
                  - luet --version
      {{ end }}

      {{ if .Values.local_luet_makeiso_binary }}
              - event: post-node-sync
                node: host
                commands:
                  - lxc file push {{ .Values.local_luet_makeiso_binary }} {{ .Values.connection }}:mocaccino-makeiso/tmp/luet-makeiso

              - event: post-node-sync
                commands:
                  - mv /tmp/luet-makeiso /usr/bin/luet-makeiso
                  - luet-makeiso --version
      {{ end }}
              - event: post-node-sync
                flags:
                  - build_iso
                commands:
                  - >-
                    cd /repo/ &&
                    if [ ! -e $TMPDIR ] ; then mkdir $TMPDIR -p ; fi &&
                    echo "Using spec file ${spec_file}..." &&
                    rm -rf /var/luet/db/repos/ -rf &&
                    luet-makeiso ${spec_file} &&
                    ls -ltr &&
                    mkdir iso &&
                    mv *.iso *.iso.* iso/

              - event: post-node-sync
                node: host
                flags:
                  - pull_iso
                commands:
                  - >-
                    eval 'rm -rf iso || true' &&
                    lxc file pull {{ .Values.connection }}:mocaccino-makeiso/repo/iso/ . --recursive
